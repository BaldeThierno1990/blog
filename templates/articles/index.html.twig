{% extends 'base.html.twig' %}

{% block title %}Articles index{% endblock %}
{% block stylesheets %}
        <link rel="stylesheet" href="/css/main.css">
  {% endblock %}

{% block body %}

 {% for article in articles %}

<div class="container mt-4">
    <div class="card p-3">
        <div class="row">
            <div class="col-md-4">
                <div class="position-relative snipimage"> <img src="{{vich_uploader_asset(article, 'imageFile')}}" class="rounded img-fluid w-100 img-responsive"> <span class="position-absolute user-timing">1 min</span> </div>
            </div>
            <div class="col-md-8">
                <div class="mt-2">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-1">{{article.titre}} </h5>
                    </div>
                    <div class="d-flex justify-content-md-start justify-content-between views-content mt-2">
                      <h5 class="mb-1">{{article.contenu|slice(0, 300)|raw}} </h5> 
                    </div>
                    <div class="d-flex flex-row mt-3"> <img src="https://i.imgur.com/rvQ3LAt.jpg" width="50" class="rounded-circle">
                        <div class="ms-2 d-flex flex-column">
                            <div class="d-flex flex-row align-items-center">
                                <h6>Thierno</h6> <span class="dots"></span>
                            </div> <span class="days-ago mt-5">Publié le {{article.createdAt|date('Y-m-d H:i:s')}}</span>
                            <a class=" card-text btn btn-success" href="{{ path('articles_show', {'id': article.id}) }}">Continuer</a>
                             <a href="{{ path('articles_edit', {'id': article.id}) }}">edit</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
   
   
</div>

 
  
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('articles_new') }}">Create new</a>
{% endblock %}
